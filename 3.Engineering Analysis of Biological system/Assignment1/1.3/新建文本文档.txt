%% 1.3  Chapra 22.17  Temperature change in a corpse
Ta=; %ambient temperature
To=; %normal body temperature at time of death, t=0;
Tx=; %body temp when body was discovered, t=x;
Tx2=; %body temp 2 hr after discovery
% Equations to compute x and K




x=     %time body found, hr
K=     %K, units = hr^-1

%define differential equation
temp=@(t,y,K)       ;
%set up time duration, tspan
tspan=[0 4];
%solve ode--if using extra parameters, use empty bracket [] before extra parameter.
[t,y]=ode45(         );

%% Plot solution
figure
plot(t,y,'-','linewidth',3);
xlabel('time (hr)')
ylabel('Body Temp (^oC)');
set(gca, 'fontsize',18,'fontweight','bold');
grid on